name: music_lyrics
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.18.6 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

  on_audio_query: ^2.6.1  #追加
  permission_handler: ^10.2.0 # 追加
  just_audio: ^0.9.31 # 追加
  flutter_riverpod: ^2.1.3  #追加
  just_audio_background: ^0.0.1-beta.9  # 追加

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:

  uses-material-design: true

  # -----フォントの追加-----
  fonts:
    - family: Noto Sans JP
      fonts:
        - asset: fonts/Noto_Sans_JP/NotoSansJP-Regular.otf
  # -----ここまで-----